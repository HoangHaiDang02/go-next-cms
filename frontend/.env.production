# Public base used by the browser
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Internal base used by SSR inside the frontend container
# Point to Docker service name so SSR can reach backend in the network
API_BASE_INTERNAL=http://backend:8080

